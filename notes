
Pipeline needs pipeline layout, vertex layouts, colour format

Pipeline Layout needs camera bind group layout, texture bind group layout

Texture Bind Group Layout needs layout entries

Layout entry needs type, count


Material is map of string to texture id
Shader specifies "texture albedo" for type and material map key?
